# WARNING: Do NOT commit real secret values here.
# This file is a placeholder — real values are injected at deploy time
# by the GitHub Actions workflow via kubectl create secret (--dry-run=client).
# Commit this file with empty/placeholder values only.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: default
  labels:
    app: moodmatch
type: Opaque
stringData:
  SPOTIFY_CLIENT_ID: ""
  SPOTIFY_CLIENT_SECRET: ""
  OPENAI_API_KEY: ""

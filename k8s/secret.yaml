# Template only — DO NOT commit real secret values.
# Create the secret out-of-band, e.g.:
#   kubectl create secret generic app-secrets \
#     --from-literal=SPOTIFY_CLIENT_ID=... \
#     --from-literal=SPOTIFY_CLIENT_SECRET=... \
#     --from-literal=OPENAI_API_KEY=...
# Or manage via Azure Key Vault + Secrets Store CSI Driver / External Secrets Operator.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app.kubernetes.io/name: applied-ai-final-project-codepath
    app.kubernetes.io/part-of: applied-ai-final-project-codepath
type: Opaque
stringData:
  SPOTIFY_CLIENT_ID: "REPLACE_ME"
  SPOTIFY_CLIENT_SECRET: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
